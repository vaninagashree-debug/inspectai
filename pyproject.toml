[tool.vercel]
entrypoint = "backend.main:app"
[project]
name = "inspectai"
version = "1.0.0"
description = "AI-powered inspection application"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "python-multipart"
]
[project]
name = "inspectai"
version = "1.0.0"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
    "python-multipart",
    "sqlalchemy",
    "requests"
]
